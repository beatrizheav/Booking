import {
  StyleSheet,
} from "react-native";
import { colorsTheme } from "../colorsTheme";

export const containers = StyleSheet.create({
  mainContainer: {
    flex: 1,
    alignItems: 'center'
  },
  calendarContainer: {
    width: '100%',
    paddingTop: '10%'
  },
})
