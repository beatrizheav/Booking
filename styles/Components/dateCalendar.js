import {
  StyleSheet,
} from "react-native"

export const containers = StyleSheet.create({
  mainContainer: {
    flex: 1,
    alignItems: 'center'
  },
  calendarContainer: {
    width: '100%',
    paddingTop: '10%'
  },
})
